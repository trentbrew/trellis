# sandbox/tml-todo — minimal teaching app

North-star `.tml` only. Ops use **generic** verbs (`remove`, `create`) — not `removeTodo`. Illustrative; not in admin allowlist yet.

**Five-surface twin** (TML · HTML · React · Vue · Svelte): [`crosswalk/`](./crosswalk/).
