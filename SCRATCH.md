Core admin rail

- Collections (+ types/schema) — records CRUD
- Storage — blobs / file fields
- Auth — users, tokens, rules
- Automations — cron + hooks (your other session)
- History — mutation / op log (debug + audit)
- API — endpoints, keys, copy-paste SDK bits
