/**
 * Headless UI Convention — shared furniture (ADR 0034).
 *
 * @module trellis/headless
 */

export type { HeadlessComponentType, HeadlessCore, RegistryEntry } from './core.js';
export { syncFromCore, toSvelteStore } from './store.js';
